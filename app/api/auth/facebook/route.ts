import {NextRequest,NextResponse} from "next/server"; import {randomBytes} from "crypto"; import {cookies} from "next/headers"; export const runtime="nodejs"; export async function GET(req:NextRequest){const id=process.env.META_APP_ID,redirect=process.env.META_REDIRECT_URI;if(!id||!redirect)return NextResponse.json({error:"Facebook OAuth is not configured"},{status:503});const state=randomBytes(32).toString("hex");(await cookies()).set("fb_oauth_state",state,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:600});const u=new URL("https://www.facebook.com/v24.0/dialog/oauth");u.searchParams.set("client_id",id);u.searchParams.set("redirect_uri",redirect);u.searchParams.set("state",state);u.searchParams.set("scope","pages_show_list,pages_read_engagement,pages_manage_posts");return NextResponse.redirect(u)}