import Link from "next/link"; import {getSession} from "@/lib/session"; export default async function Home(){const s=await getSession();return <main className="shell"><section className="hero"><div><span className="badge">FB POST MANAGER</span><h1>โพสต์ Facebook แบบอัตโนมัติ</h1><p>เชื่อมต่อ Facebook Page เขียนโพสต์ และตั้งเวลาเผยแพร่จากเว็บเดียว</p><Link className="button" href={s?"/dashboard":"/api/auth/facebook"}>{s?"เข้าสู่แดชบอร์ด":"เชื่อมต่อ Facebook"}</Link></div></section></main>}